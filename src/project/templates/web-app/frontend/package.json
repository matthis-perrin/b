{
  "name": "{{PROJECT_NAME}}-frontend",
  "license": "UNLICENSED",
  "scripts": {
    "build": "NODE_ENV=production webpack build",
    "start": "NODE_ENV=development webpack serve"
  },
  "prettier": "@matthis/prettier-config",
  "eslintConfig": {
    "extends": [
      "@matthis/eslint-config-web"
    ]
  },
  "devDependencies": {
    "@matthis/eslint-config-web": "latest",
    "@matthis/prettier-config": "latest",
    "@matthis/tsconfig-web": "latest",
    "@matthis/webpack-web": "latest",
    "@types/react": "{{REACT_VERSION}}",
    "@types/react-dom": "{{REACT_VERSION}}",
    "@types/styled-components": "{{STYLED_COMPONENTS_TYPES_VERSION}}"
  },
  "dependencies": {
    "react": "{{REACT_VERSION}}",
    "react-dom": "{{REACT_VERSION}}",
    "react-router": "{{REACT_ROUTER_VERSION}}",
    "react-router-dom": "{{REACT_ROUTER_VERSION}}",
    "styled-components": "{{STYLED_COMPONENTS_VERSION}}"
  }
}
