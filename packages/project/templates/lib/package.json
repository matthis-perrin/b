{
  "name": "{{PROJECT_NAME}}",
  "license": "UNLICENSED",
  "scripts": {
    "lint": "eslint --ext ts,tsx src",
    "tsc": "tsc -p tsconfig.json"
  },
  "prettier": "@matthis/prettier-config",
  "eslintConfig": {
    "extends": [
      "@matthis/eslint-config-lib"
    ]
  },
  "devDependencies": {
    "@matthis/eslint-config-lib": "{{ESLINT_CONFIG_VERSION}}",
    "@matthis/prettier-config": "{{PRETTIER_CONFIG_VERSION}}",
    "@matthis/tsconfig-lib": "{{TSCONFIG_VERSION}}"
  },
  "dependencies": {
  }
}
